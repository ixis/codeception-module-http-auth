#
# Codeception acceptance test suite configuration for codeception-module-http-auth
#

class_name: WebGuy
modules:
    enabled:
        - PhpBrowser
        - HttpAuth
    config:
        PhpBrowser:
            url: 'http://d7.drupal.dev:8080'
        HttpAuth:
            username: testuser
            password: testpassword!
